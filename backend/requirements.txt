# ATS Resume Scorer — Backend Requirements
# Install with: pip install -r requirements.txt
#
# Post-install step:
# Download spaCy model: python -m spacy download en_core_web_md

# ── Web Framework ──
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9
python-dotenv>=1.0

# ── NLP & ML ──
spacy>=3.7,<4.0
sentence-transformers>=2.5
numpy>=1.26
rapidfuzz>=3.6

# ── File Parsing ──
pdfplumber>=0.11
PyPDF2>=3.0
python-docx>=1.1
python-magic>=0.4.27; sys_platform != 'win32'
python-magic-bin>=0.4.14; sys_platform == 'win32'

# ── PDF Report Generation ──
jinja2>=3.1
weasyprint>=60.0
reportlab>=3.6.0

# ── External Services & Auth ──
httpx>=0.27
groq>=0.13
PyJWT[crypto]>=2.8
